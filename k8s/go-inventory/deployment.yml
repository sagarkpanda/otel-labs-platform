apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-inventory
spec:
  replicas: 1

  selector:
    matchLabels:
      app: go-inventory

  template:
    metadata:
      labels:
        app: go-inventory

    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault

      containers:
        - name: go-inventory
          image: ghcr.io/sagarkpanda/go-inventory

          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            capabilities:
              drop:
                - ALL

          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi

          ports:
            - containerPort: 8080